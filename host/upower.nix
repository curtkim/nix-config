{
    services.upower = {
      enable = true;
      # 추가 설정 (선택사항)
      # percentageLow = 10;        # 배터리 낮음 임계값
      # percentageCritical = 3;    # 배터리 위험 임계값
      # percentageAction = 2;      # 자동 동작 임계값
      # criticalPowerAction = "PowerOff";  # 위험 시 동작
    };
}
